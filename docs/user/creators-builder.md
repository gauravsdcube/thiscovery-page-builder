# Builder and sections

The **Page builder** tab is where you add sections and arrange the layout. Changes are not live until you **Save page**.

## Layout

At the top of the canvas, choose how columns work:

| Layout | Columns |
| --- | --- |
| Main | One main column (plus full-width sections above) |
| Left | Left sidebar + main |
| Right | Main + right sidebar |
| Both | Left, main, and right |

**Full width** sections (typically a Hero) sit above the columns.

## Adding sections

Use the palette on the left. Drag a block onto a region, or click it to add. Drag cards to reorder. Open a card to edit.

**Containers** hold several blocks in a grid. You cannot put a container inside another container.

## Section types

### Layout

| Type | Use for |
| --- | --- |
| Hero | Title, intro, and a large image at the top |
| Container | A grid of other sections |
| Image | A picture. Tick it as the collection / directory card image if you want it instead of the hero |

### Content

| Type | Use for |
| --- | --- |
| Rich text | Body copy (Thiscovery Editor: headings, tables, images, media) |
| Video embed | A YouTube or Vimeo video (paste the watch URL or embed iframe) |
| Accordion | Expand/collapse items (FAQs) |
| Callout | A highlighted note or warning |
| Downloads | Files people can save |
| Button | A call-to-action. Presets include link, page, form, space, login/register, mailto/tel, scroll, or a custom URL |

### Engagement

| Type | Use for |
| --- | --- |
| Survey CTA | Link out to a Thiscovery Form |
| Quick poll | Embed a Thiscovery Forms poll so people can vote on the page |
| Phases | A timeline of stages |
| Events | A list of events you type in (or upcoming Calendar events on a collection) |
| Team | People cards |
| Contact | Contact cards (email button) |
| Updates | “Get updates” email signup |
| Comments | Public or member comments (moderation) |
| Collection | List other pages, forms, or spaces as cards |

### Space widgets

These need a **Bound Space** on Settings. They show that space’s stream, tasks, files, gallery, or calendar on the page.

When the page-builder module is enabled on a space, ordinary members do not use the default space stream; put a **Space stream** block on a page instead.

## Colours

Each section can override background and text colour. Page blocks otherwise follow the Thiscovery theme.

## Next

See [Page settings](creators-settings.md) for the bound Space, and [Comments, forms, and space widgets](creators-engagement.md) for comments, polls, and surveys.
